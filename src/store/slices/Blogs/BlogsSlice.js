import { createSlice } from '@reduxjs/toolkit'

// const initialState = {
//     'name' : 'tomahalil'
//   }

  export const blogs = createSlice({
    name: 'blogs',
    initialState:{},
    reducers: {
     
    },
  })

  export default blogs.reducer